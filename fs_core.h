#ifndef FS_CORE_H
#define FS_CORE_H

#include <stddef.h>

//fnunzione format
void fs_format(char* memory_ptr, size_t size);

/*
mancano:
    mkdir
    cd
    ls
    touch
    cat
    append
    rm
    close
    */
#endif